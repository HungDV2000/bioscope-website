/**
 * Sự kiện báo "phiên đăng nhập vừa đổi" giữa các thành phần chạy ở trình duyệt.
 * Đăng nhập trong widget chat xong thì nút tài khoản ở header đọc lại ngay,
 * khỏi phải tải lại trang.
 */
export const SESSION_CHANGED = 'bs:session-changed'
