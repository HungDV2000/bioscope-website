import type { CollectionConfig } from 'payload'

/** Auto-generated custom type collections — run `pnpm ct:apply` after publishing definitions. */
export const generatedCustomTypeCollections: CollectionConfig[] = []
